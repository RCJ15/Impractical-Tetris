using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Tetris
{
    /// <summary>
    /// A grid consisting of multiple <see cref="PlayfieldLine"/>. When all pieces have been dropped and all have stopped moving, then a check for every single line is started.
    /// </summary>
    public class PlayfieldGrid : MonoBehaviour
    {


        private void Start()
        {
        
        }
    
        private void Update()
        {
        
        }
    }
}
